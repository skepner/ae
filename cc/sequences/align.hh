#pragma once

// ======================================================================

namespace ae::sequences
{
    struct RawSequence;

    // returns if sequence was translated
    bool align(RawSequence& sequence);
}

// ======================================================================
